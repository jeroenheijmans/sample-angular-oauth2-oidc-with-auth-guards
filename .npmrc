# Supply-chain cooldown: do not install package versions younger than 7 days.
# Applies at resolution time (npm install/update/ng update); `npm ci` in CI is
# unaffected since it installs the vetted package-lock.json verbatim.
# Requires npm >= 11.10.0. See `npm help config` (min-release-age).
min-release-age=7
